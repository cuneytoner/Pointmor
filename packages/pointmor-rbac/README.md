# @pointmor/rbac

Kiracı izin sözlüğü ve rol → izin haritası. `apps/admin-web` ve `apps/api` aynı paketi kullanır.

- **Config:** `src/rbac-config.ts`
- **Test:** `npm run test`

Detaylı checklist: `docs/41-ref-003-rbac-config-and-tests.md`.
