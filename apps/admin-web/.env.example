# Vite bu dosyayı örnek olarak tutar. Yerelde kopyalayın:
#   copy .env.example .env.local   (PowerShell: Copy-Item .env.example .env.local)
# Ardından `npm run dev` — böylece konsolda VITE_API_BASE_URL uyarısı çıkmaz.
VITE_API_BASE_URL=http://127.0.0.1:3000
