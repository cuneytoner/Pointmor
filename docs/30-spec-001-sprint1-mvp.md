# Sprint 1 — MVP hedef dokümanı (arşiv)

**Durum:** Bu spesifikasyondaki hedef ürün daha önce **`apps/doc-web`** altında uygulanıyordu; bu uygulama **Royalty monoreposundan kaldırıldı**. Aşağıdaki maddeler, ileride yeni bir kod tabanı yazılırken **referans** içindir.

**Özet hedef (konsept):** Uygulama içi başlık ile minimal doküman modeli → HTML + tema + logo → PDF → barındırılmış sayfa → public / unlisted paylaşım.

**Canlı plan ve sprint haritası:** [`10-plan-001-document-saas.md`](./10-plan-001-document-saas.md).

---

*Önceki detaylı API ve klasör taslağı bu dosyada tutuluyordu; uygulama silindiği için teknik satır satır checklist yerine yukarıdaki yön + ana plan belgesi kaynak alınmalıdır.*
