name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Prisma generate
        run: npm --workspace apps/api run db:generate

      - name: Prisma validate
        run: npx prisma validate --schema apps/api/prisma/schema.prisma

      - name: Admin i18n check
        run: npm --workspace apps/admin-web run check:i18n
